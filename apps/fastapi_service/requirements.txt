# ARIES FastAPI Service — Python Dependencies
# Production inference only (no training dependencies)

# ── Web Framework ─────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
uvloop>=0.21.0
httptools>=0.6.0
pydantic>=2.10.0
pydantic-settings>=2.7.0

# ── ONNX Runtime (CPU) ───────────────────────────────────────────────
onnxruntime>=1.20.0
numpy>=1.26.0,<2.0.0

# ── Tokenizers ────────────────────────────────────────────────────────
tokenizers>=0.21.0
transformers>=4.47.0

# ── Kafka ─────────────────────────────────────────────────────────────
aiokafka>=0.12.0

# ── PostgreSQL ────────────────────────────────────────────────────────
asyncpg>=0.30.0

# ── Redis ─────────────────────────────────────────────────────────────
redis>=5.2.0

# ── S3 / MinIO ───────────────────────────────────────────────────────
aioboto3>=13.0.0
botocore>=1.35.0

# ── Structured Logging ───────────────────────────────────────────────
structlog>=24.4.0

# ── HTTP Client (for enrichment calls) ───────────────────────────────
httpx>=0.28.0

# ── Feature engineering (TargetEncoder for triage inference) ─────────
pandas>=2.0.0
joblib>=1.3.0
category-encoders>=2.6.0

# ── SLM inference (llama-cpp-python installed separately in Dockerfile) ──
# Built with CMAKE_ARGS="-DGGML_CUDA=ON" FORCE_CMAKE=1 for full GPU offload.
# Not listed here to avoid pip overwriting the CUDA build. No entry here.
